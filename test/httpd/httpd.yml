version: "3.9"

services:
  nginx:
    image: httpd:latest
    deploy:
      replicas: 1
      placement:
        constraints:
          - "node.role == worker"    # Ensures Nginx runs on worker1 or worker2
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.httpd.rule=Host(`httpd.blog.zerops.ir`)"
        - "traefik.http.routers.httpd.entrypoints=web"
        - "traefik.http.services.httpd.loadbalancer.server.port=80"
        - "traefik.docker.network=web_net"
    networks:
      - web_net

networks:
  web_net:
    external: true
