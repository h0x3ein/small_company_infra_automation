version: '3.8'

volumes:
  ws_data: 
    driver: local

services:
  wordpress:
    image: wordpress:latest
  
    environment:
      WORDPRESS_DB_HOST: haproxy:3306  # Connect to HAProxy for load balancing
      WORDPRESS_DB_USER: wordpress
      WORDPRESS_DB_PASSWORD: wordpresspassword
      WORDPRESS_DB_NAME: wordpress
    volumes:
      - ws_data:/var/www/html
    networks:
      - web_net
      - internal-network
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      labels:
        # HTTP router for ws.blog.zerops.ir
        - traefik.enable=true
        - traefik.docker.network=web_net
        - traefik.http.routers.wordpress.rule=Host(`ws.blog.zerops.ir`)
        - traefik.http.routers.wordpress.entrypoints=http
        - traefik.http.routers.wordpress.service=wordpress-secure-service
        - traefik.http.services.wordpress-secure-service.loadbalancer.server.port=80
        
        # HTTPS router for ws.blog.zerops.ir
        - traefik.http.routers.wordpress.middlewares=https-redirect
        - traefik.http.routers.wordpress-secure.entrypoints=https
        - traefik.http.routers.wordpress-secure.rule=Host(`ws.blog.zerops.ir`)
        - traefik.http.routers.wordpress-secure.tls=true
        - traefik.http.routers.wordpress-secure.tls.options=default
        - traefik.http.routers.wordpress-secure.service=wordpress-secure-service
        - traefik.http.routers.wordpress-secure.tls.certresolver=mycert


networks:
  web_net:
    external: true
  internal-network:
    external: true

